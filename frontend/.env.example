# Real API integration is the default. Set true only for the self-contained product demo.
VITE_DEMO_MODE=false
VITE_API_URL=/api/v1
# Used by Vite's local /api HTTP and WebSocket proxy.
VITE_API_PROXY_TARGET=http://localhost:8080
